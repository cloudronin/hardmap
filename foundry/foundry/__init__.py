"""Foundry — the synthetic census (proof-space line, project 5).

Builds a multi-charge atlas over a roster no human chose (generated, stratified constraint languages) to test
whether Eightfold's canon structure reproduces free of selection bias. Reuses the Eightfold atlas kernel
(Phase K: the ChargeSpec-parametrized validator + Crucible-hardened harness) and the Proof-Census measurement
apparatus. One-way dependency: foundry imports eightfold + proofcensus; it never modifies them.
"""
__version__ = "0.1.0"
